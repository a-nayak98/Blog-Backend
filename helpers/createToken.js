// const jwt = require("jsonwebtoken");

// const createToken = ({userId}) => {
//   const data = jwt.sign({ id: userId }, process.env.secretKey);
//   return data;
// };

// module.exports = { createToken };
